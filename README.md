# 個人智識庫 AI Agent

一個基於 Python 和 LangChain 的個人智識庫 AI Agent 應用，可以幫助您管理、搜尋和查詢個人知識庫。

## 功能特色

- 📚 **多格式文件支援**：支援 TXT、PDF、DOCX、Markdown 等格式
- 🔍 **智能搜尋**：使用向量資料庫進行語義搜尋
- 🤖 **AI Agent**：基於 LangChain 的智能助手，可以理解自然語言問題
- 💾 **持久化儲存**：使用 ChromaDB 儲存文件向量
- 💬 **對話記憶**：支援多輪對話，記住上下文
- 🛠️ **工具系統**：內建知識搜尋和文件摘要工具

## 系統架構

```
knowledge_base/
├── __init__.py              # 套件初始化
├── vector_store.py          # 向量資料庫模組
├── document_processor.py    # 文件處理模組
├── agent.py                 # AI Agent 核心
├── tools/                   # Agent 工具
│   ├── __init__.py
│   └── knowledge_tools.py   # 智識庫工具
├── data/                    # 資料儲存目錄
└── documents/               # 文件目錄
```

## 安裝步驟

### 1. 克隆專案

```bash
git clone <repository-url>
cd ai_agent
```

### 2. 建立虛擬環境

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows
```

### 3. 安裝依賴

```bash
pip install -r requirements.txt
```

### 4. 配置環境變數

複製 `.env.example` 為 `.env` 並填入您的 OpenAI API 金鑰：

```bash
cp .env.example .env
```

編輯 `.env` 檔案：

```env
OPENAI_API_KEY=your_openai_api_key_here
```

## 使用方法

### 啟動應用程式

```bash
python main.py
```

### 可用指令

1. **新增文件到智識庫**
   ```
   add <路徑>
   ```
   例如：
   - `add ./knowledge_base/documents/my_notes.txt`
   - `add ./knowledge_base/documents/` (新增整個目錄)

2. **向智識庫提問**
   ```
   query
   ```
   然後輸入您的問題

3. **清除對話記憶**
   ```
   clear
   ```

4. **顯示支援的文件格式**
   ```
   formats
   ```

5. **顯示幫助選單**
   ```
   help
   ```

6. **退出程式**
   ```
   exit
   ```

### 使用範例

```bash
# 新增文件
👤 請輸入指令: add ./knowledge_base/documents/

# 提問
👤 請輸入指令: query
💭 請輸入您的問題: 什麼是機器學習？

# 或直接輸入問題
👤 請輸入指令: 深度學習和機器學習有什麼區別？
```

## 支援的文件格式

- `.txt` - 純文字檔案
- `.pdf` - PDF 文件
- `.docx` - Word 文件
- `.md` - Markdown 文件

## 技術棧

- **Python 3.8+**
- **LangChain** - AI Agent 框架
- **OpenAI GPT-4** - 大型語言模型
- **ChromaDB** - 向量資料庫
- **OpenAI Embeddings** - 文本嵌入模型

## 專案結構說明

- `main.py` - 主程式入口
- `knowledge_base/vector_store.py` - 向量資料庫管理
- `knowledge_base/document_processor.py` - 文件載入和分割
- `knowledge_base/agent.py` - AI Agent 核心邏輯
- `knowledge_base/tools/` - Agent 工具集
- `requirements.txt` - Python 依賴套件
- `.env` - 環境變數配置

## 注意事項

- 需要有效的 OpenAI API 金鑰
- 首次使用時需要新增文件到智識庫
- 文件會被分割成較小的片段以提高搜尋效率
- 向量資料庫會持久化儲存在 `knowledge_base/data/chroma/` 目錄

## 授權

MIT License

